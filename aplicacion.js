/* const { suma } = require('./example'); */

import sumar, { restar, multiplicar, PI } from "./example.js";

const result1 = sumar(10, 10);
const result2 = restar(10, 10);
const result3 = multiplicar(10, 10);

const result4 = sumar(PI, 10);